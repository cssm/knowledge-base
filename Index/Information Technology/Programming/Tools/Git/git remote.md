```bash
git remote add $NAME $LINK
git push -u $REMOTE_NAME $REMOTE_BRANCH # Publish the current branch to a remote repository, setting the remote branch name

git remote -v # list all remotes

git remote remove $NAME
```