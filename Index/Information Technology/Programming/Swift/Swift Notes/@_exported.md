# @_exported

```swift
// A and B is swift module

// to use names from A in B we need explisitly write ervery time 
import A

// if we write like this
@_exported import A

// now we can use names from A in every B file
```