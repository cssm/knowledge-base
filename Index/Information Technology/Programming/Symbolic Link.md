
```bash
# Create symbolic link
ln -s /path/to/original /path/to/where/symlink
	  └───────┬─────────┘
              └── the path to the original file/folder
                		 can use . or ~ or other relative paths
```
