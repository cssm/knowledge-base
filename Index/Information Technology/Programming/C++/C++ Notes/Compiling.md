# Compiling

`#include` just paste all it code in place where it defined

Only `.cpp` files are recompiled when building multy-file project

When `.h` file are changing - we need to recompile all `.cpp` files in which it imported