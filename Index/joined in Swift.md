- can be applied to any `Array`
- Work's as you expect with [[Swift Strings]]
	```swift
	debugPrint(["1"].joined(separator: "; ")) // "1"
	debugPrint(["1", "2"].joined(separator: "; ")) // "1; 2"
	```